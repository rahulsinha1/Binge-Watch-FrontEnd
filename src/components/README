Router is in index.js


User identity is following way to get:

localStorage.getItem("username")

and your out put will be current log in user's name

same thing you can change "username" to id, role

